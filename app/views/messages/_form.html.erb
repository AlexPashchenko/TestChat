<%= form_for([@room, @room.messages.build], remote: true, html: {class: :save_message})   do |f| %>
    <fieldset>
      <%= f.text_area :text, placeholder: 'Add your message..', class: 'textarea' %>
      <%= f.submit class: 'btn btn-primary' %>

    </fieldset>
<% end %>